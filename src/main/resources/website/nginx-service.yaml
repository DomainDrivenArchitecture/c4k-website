kind: Service
apiVersion: v1
metadata:
  name: NAME
  namespace: default
  labels:
    app: NAME
    app.kubernetes.part-of: NAME-website
spec:
  selector:
    app: nginx
  ports:
    - name: nginx-http
      port: 80
