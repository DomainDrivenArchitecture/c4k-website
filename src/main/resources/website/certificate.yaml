apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: CERTNAME
  namespace: default
spec:
  secretName: CERTNAME
  commonName: FQDN
  duration: 2160h # 90d
  renewBefore: 360h # 15d
  dnsNames:
  - FQDN
  issuerRef:
    name: staging
    kind: ClusterIssuer
    